function love.conf(t)
    -- t.window.fullscreen = true       -- Enable fullscreen
    -- t.window.fullscreentype = "desktop" -- Use desktop fullscreen (keeps native resolution)
    t.window.vsync = 1               -- Enable vsync (optional)
end
